problem 1:if we have to solve range sum query then we will use prefix sum
original array= 1 2 3 4 5
prefix array= 1 3 6 10 15
prefix[i]=prefix[i-1]+arr[i]
l=1 ,r=3
prefix[0]=0
now we have to solve the range sum:
range sum=prefix[r]-prefix[l-1] = 6-0=6

problem statement 2: we have to rotate the array 
sample input:
1 2 3                    7 4 1
4 5 6   = rotate by 3    8 5 2
7 8 9                    9 6 3

transpose of matrix:
for(i=0;i<n;i++)
{
    for(j=0;j<n;j++)
    {
        temp=arr[i][j];
        arr[i][j]=arr[j][i];
        arr[j][i]=temp;
    }
}
//reverse the matrix
for(i=0;i<n;i++)
{
    for(j=0;j<n/2;j++)
    {
        temp=arr[i][j];
        arr[i][j]=arr[i][n-j-1];
        arr[i][n-j-1]=temp;
    }
}


problem statement 3:
we have to implements the an array and count frequency of all array elements and print that elements which frequency is more than n/4 times;


problem statement 4:

we have to multiply all even row by -1 and reverse all row

Todays home work
1: Leaders in an array
2: Spiral Travelsal of matrix
3: Longest consecutive increasing subarray
4: frequency sort
5: Matrix diagonal difference
6: Rotate matrix by 180
7: sort 0s, 1s, and 2s